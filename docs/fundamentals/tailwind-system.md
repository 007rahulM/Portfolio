# Tailwind System

Use spacing scale, responsive prefixes, and semantic tokens instead of raw hex values.

## Why this matters in companies

Consistent spacing and colors create professional polish with less design debt.
